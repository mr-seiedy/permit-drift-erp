# Logs generated for the paper

Each folder holds log.csv.gz, log.xes.gz, metadata.csv and ground_truth.json.

metadata.csv is deliberately misleading: it records when each activity was
declared, not when it first produced an event. ground_truth.json is the truth.
